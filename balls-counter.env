# balls-counter environment configuration
# Edit this file to change settings without modifying the service file.

# Path to the config file (relative to WorkingDirectory or absolute)
CONFIG=configs/live.json

# Web server port
WEB_PORT=8080

# YOLO model path (relative to WorkingDirectory or absolute)
YOLO_MODEL=models/yolo_ball_detector.pt
